describe('<FadeIn />', () => {
  xit('should ease in for the timing curve', () => {});

  xit('should set a default duration', () => {});

  xit('should override the default duration', () => {});

  xit('should fill the animation backwards so it starts hidden', () => {});

  xit('should respect reduced motion', () => {});

  xit('should default to playing the animation', () => {});

  xit('should pause the animation', () => {});

  xit('should not delay the animation by default', () => {});

  xit('should delay the animation', () => {});
});
